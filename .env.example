# Variáveis de Ambiente - Estratégia Viva

# Ambiente de execução
NODE_ENV=production

# Porta da API interna (Node.js/Express)
# O Nginx faz proxy de /api/* para esta porta
PORT=3001

# Opcional: Debug mode (não recomendado em produção)
# DEBUG=false
